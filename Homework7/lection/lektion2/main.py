import html as hc
import xml as x
import os
import start_data as sd

os.system('cls') # при компиляции на Linux или MaxOS необходимо 'cls' за менить на 'clear'global x
# print(x.new_create())
hc.new_create(x.new_create(sd.collection()))